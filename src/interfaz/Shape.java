package interfaz;

public interface Shape {
	//se puede omitir las palabras public y abstract
	double getArea(); // Método abstracto que debe ser 
	//implementado por una clase concreta
    double getPerimeter(); // Otro método abstracto que
    //debe ser implementado por una clase concreta
}
